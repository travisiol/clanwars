import { Board } from "@/components/Board";

/*
 * One page. The map is the product, the pitch sits over it, and everything
 * else opens on top and closes back to the board.
 */
export default function Home() {
  return <Board />;
}
